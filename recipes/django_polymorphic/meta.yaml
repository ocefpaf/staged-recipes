{% set version = "0.7.1" %}

package:
    name: django_polymorphic
    version: {{ version }}

source:
    fn: django_polymorphic-{{ version }}.tar.gz
    url: https://pypi.python.org/packages/source/d/django_polymorphic/django_polymorphic-{{ version }}.tar.gz
    md5: c6999aa8aa4d5a992e2664f050a8e858

build:
    number: 0
    script: python setup.py install --single-version-externally-managed --record=record.txt
    preserve_egg_dir: True

requirements:
    build:
        - python
        - setuptools
    run:
        - python
        - django

#test:
    #requires:
        #- coverage
    #files:
        #- runtests.py
    #commands:
        #- cp runtests.py $SRC_DIR && cd $SRC_DIR && coverage run --source=polymorphic runtests.py

about:
    home: https://github.com/chrisglass/django_polymorphic
    license: BSD License
    summary: Seamless Polymorphic Inheritance for Django Models

extra:
    recipe-maintainers:
        - kwilcox
